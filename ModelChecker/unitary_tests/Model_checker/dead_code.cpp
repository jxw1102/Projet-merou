int f(int q) {
    return q+2;
}

int main(int argc, char** argv) {
	int j = 0, q = 2; 
    if (j == 2)
    	f(q);
    
    if (false){
    	q = 7;
    }
    else {
    	j = 3;
    	for (int r=0 ; ; r++)
    		if (j = q) {
    			q = j;
    		}
    	switch (5 + 6 * 3) {
    		case 2: break;
    	}
    } 
    
    while (j = 3 + 4*(6 - 7));

    while (true) {
    	if (0);
    	int x = 5;
    	switch (x) {
    		case 4  :
    		case 3  : if (q == q) x += 2;
    		default : if (q = 6) x += 5;
    	}
    }

    int d;
    
    return 0;
}
